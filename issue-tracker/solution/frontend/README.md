# Issue Tracker — frontend (Angular)

```bash
npm install
npm start       # app on http://localhost:4200, /api proxied to the backend
```

Standalone components and built-in control flow (`@if`, `@for`). All API access
goes through `issues/issue.service.ts`.
