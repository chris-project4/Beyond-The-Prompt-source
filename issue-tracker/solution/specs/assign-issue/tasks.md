# Tasks: Assign an issue

- [ ] `issueRepository.assign(id, assignee)` (return null on no change).
- [ ] `parseAssign` in `validation.ts`.
- [ ] `issueService.assignIssue` (throw `notFound` on null).
- [ ] `POST /:id/assign` route.
- [ ] Frontend: `IssueService.assign` + "Assign to me" in the list.
- [ ] Tests for acceptance criteria 1-3.
