# Issue Tracker — frontend (start here)

A booting Angular shell with one placeholder route. Build the issues list and
create form as you implement the feature, then wire them into the router.

```bash
npm install
npm start       # http://localhost:4200
```
