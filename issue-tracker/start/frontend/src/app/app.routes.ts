import { Routes } from '@angular/router';
import { HomeComponent } from './home.component';

// One placeholder route. Add the issues routes as you build the feature.
export const routes: Routes = [{ path: '', component: HomeComponent }];
