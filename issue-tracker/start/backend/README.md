# Issue Tracker — backend (start here)

A booting skeleton: `GET /api/health` works, the database opens, nothing else
exists yet. Build "create and list issues" from `../specs/create-and-list-issues/`,
following the conventions in `../CLAUDE.md`.

```bash
npm install
npm run dev     # http://localhost:3000/api/health
```
